{
    "name": "nzta/googlemap-leafletfield",
    "description": "Provides a form field type allowing users to draw features on a map, the features are stored as geoJSON objects in a single database field",
    "type": "silverstripe-vendormodule",
    "homepage": "https://github.com/nzta/googlemap-leafletfield",
    "keywords": [
        "silverstripe",
        "leaflet",
        "map"
    ],
    "license": "BSD-3-Clause",
    "support": {
        "issues": "https://github.com/nzta/googlemap-leafletfield/issues"
    },
    "authors": [
        {
            "name": "Robby",
            "email": "channels@nzta.govt.nz"
        }
    ],
    "require": {
        "benmanu/silverstripe-leafletfield": "^2 | ^3"
    },
    "autoload": {
        "psr-4": {
            "NZTA\\LeafletField\\": "src/"
        }
    },
    "extra": {
        "expose": [
            "client"
        ]
    },
    "abandoned": "benmanu/silverstripe-leafletfield",
    "_comment": [
        "This package alters the front-end requirements (via the Silverstripe Requirements class) only",
        "Use benmanu/silverstripe-leafletfield directly since a hook was added to do this via Extension in 2025"
    ]
}
